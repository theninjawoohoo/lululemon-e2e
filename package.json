{
  "name": "playwright-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "build": "tsc",
    "pw:run": "playwright test --config 'playwright.config.ts' --headed "
  },
  "dependencies": {
    "msw": "^2.8.2",
    "playwright": "^1.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^14.0.0",
    "typescript": "^4.0.0"
  }
}
